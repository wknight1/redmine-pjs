services:
  redmine:
    build:
      context: .
      target: application
    environment:
      - REDMINE_DB_POSTGRES=db
      - REDMINE_DB_DATABASE=redmine
      - REDMINE_DB_PASSWORD=${DB_PASSWORD}
      - REDMINE_SECRET_KEY_BASE=${REDMINE_SECRET_KEY}
      - TZ=Asia/Seoul
    volumes:
      - redmine_files:/usr/src/redmine/files
      - redmine_plugins:/usr/src/redmine/plugins
      - redmine_themes:/usr/src/redmine/public/themes
    # [한글 완전 정복 파이프라인]
    command: >
      sh -c "/docker-entrypoint.sh bundle exec rake redmine:plugins:migrate RAILS_ENV=production && 
             bundle exec rails runner \"ActiveRecord::Base.connection.execute('CREATE EXTENSION IF NOT EXISTS pg_trgm')\" -e production &&
             bundle exec rails runner /usr/src/redmine/init_all.rb -e production &&
             /docker-entrypoint.sh rails server -b 0.0.0.0"
    depends_on:
      db:
        condition: service_healthy
    restart: always
    deploy:
      resources:
        limits:
          memory: 2G

  db:
    build:
      context: .
      target: database
    environment:
      - POSTGRES_USER=redmine
      - POSTGRES_DB=redmine
      - POSTGRES_PASSWORD=${DB_PASSWORD}
      - POSTGRES_INITDB_ARGS=--encoding=UTF-8 --locale-provider=icu --icu-locale=ko_KR.UTF-8 --lc-collate=ko_KR.UTF-8 --lc-ctype=ko_KR.UTF-8
    command: >
      postgres -c shared_buffers=512MB -c work_mem=16MB -c max_connections=200 -c default_text_search_config=pg_catalog.simple
    volumes:
      - db_data:/var/lib/postgresql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U redmine"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: always

volumes:
  db_data:
  redmine_files:
  redmine_plugins:
  redmine_themes:

networks:
  default:
    name: redmine_pjs_net